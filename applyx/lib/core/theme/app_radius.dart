/// ApplyX design tokens — border radius system.
///
/// Source of truth: design.md § "Final spacing / shape rules"
class AppRadius {
  AppRadius._();

  /// Small controls — 8px
  static const double sm = 8.0;

  /// Controls, input fields — 12px
  static const double md = 12.0;

  /// Control emphasis — 14px
  static const double mdLg = 14.0;

  /// Major cards — 18px
  static const double lg = 18.0;

  /// Large sheets / modals — 24px
  static const double xl = 24.0;

  /// Pill / chip — 999px
  static const double pill = 999.0;
}
